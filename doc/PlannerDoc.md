# 📅 МОДУЛЬ PLANNER v2.0 — ПАТЧ К ТЕКУЩЕЙ РЕАЛИЗАЦИИ

Этот документ НЕ переписывает модуль с нуля, а **дополняет и уточняет** существующую спецификацию  
«📅 МОДУЛЬ PLANNER – Техническая спецификация LEORA».  

PLANNER уже очень богат по функциям (Tasks, Goals, Habits, Focus, Calendar, AI, голос, аналитика).  
Задача этого патча — сделать его более **связным, управляемым и готовым к реальной работе**, не ломая дизайн.

---

## 1. Что оставляем как есть

Из текущего описания модуля PLANNER считаем **каноничными** и не меняем:

- 📱 Общие параметры:
  - Название раздела: **PLANNER**
  - Позиция в таб-навигации: 3-я вкладка
  - Основные вкладки: **Tasks / Goals / Habits**
  - Доп. функции: Календарь, Фокус-режим, AI-рекомендации, голос, аналитика.

- 🧭 HEADER:
  - Заголовок «PLANNER», чёрный фон, справа иконки: поиск (🔍), календарь (📅), AI (🎯), настройки (⚙️).

- 🧩 Система вкладок:
  - TabBar с 3 вкладками (Tasks / Goals / Habits), иконки, поведение Active/Inactive, underline-анимация.

- 📝 Вкладка **Tasks**:
  - Группировка задач по времени дня: УТРО / ДЕНЬ / ВЕЧЕР.
  - Карточка задачи: название, время/длительность, контекст (@дом/@работа/…), приоритет, энергия, чекбокс, визуальные индикаторы.
  - Кнопка «Фокус» на задаче.
  - AI-подсказки внутри карточки.

- 🎯 Вкладка **Goals**:
  - Карточки целей с прогрессом, Progress bar, деталями (прогресс, осталось, темп, прогноз).
  - Типы целей: Финансовые, Личные, Профессиональные.

- ✅ Вкладка **Habits**:
  - Карточка привычки, иконка, streak, календарная сетка, статистика, кнопка «Отметить сегодня», AI-советы.

- 🧾 Модальные окна:
  - Add Task, Add Goal, Add Habit — с полным набором полей, иконок, приоритетов, энергии, напоминаний, вех и т.д.

- ⏱️ Focus Mode:
  - Выбор техники (Pomodoro/Блоки/Своя), блокировки уведомлений, запуск с задач.

- 📅 Calendar View:
  - Режимы Месяц/Неделя/День/Список, индикаторы задач/привычек/целей, статистика снизу.

- ➕ FAB:
  - Кнопка «+» (Start Focus / Add Task).

- 🤖 AI-функции:
  - AI-рекомендации для задач, привычек, целей; голосовые команды; аналитика продуктивности и привычек.

**Важно:**  
Патч не меняет визуальную структуру и идею. Мы добавляем **связность, логику и поведение**.

---

## 2. Новые ключевые функции PLANNER v2.0

### 2.1. Единый «Дневной сценарий» (Daily Flow)

PLANNER должен работать как **единый сценарий дня**, а не набор отдельных экранов.

**Требование:**

- На вкладке **Tasks** (или через отдельный Daily-header) явно отражать:
  - Сегодняшнюю дату и краткий статус:
    - «3 важные задачи • 2 привычки • 1 шаг по цели».
- При открытии PLANNER по умолчанию:
  - показывается **«Сегодня»**:
    - задачи на сегодня,
    - привычки на сегодня,
    - ближайшие шаги по целям.

**Цель:** пользователь понимает, *что именно нужно сделать сегодня*, а не просто видит списки.

---

### 2.2. Связка Tasks ↔ Goals ↔ Habits

Сейчас Tasks, Goals и Habits описаны богато, но **логическая связь между ними не формализована**.

**Нужно ввести отношения:**

- Каждая задача может быть:
  - привязана к одной цели (`goalId`) или быть «разовой».
- Привычки могут:
  - поддерживать одну или несколько целей (`linkedGoalIds`).
- Цели:
  - показывают, сколько задач и привычек на них завязано.

**UI-требования:**

- На карточке задачи (Tasks):
  - маленький бейдж: «связано с целью X» (если есть связь).
- На карточке цели (Goals):
  - блок:  
    - «Связанные задачи (N/выполнено M)»  
    - «Связанные привычки (N активных)».
- На карточке привычки (Habits):
  - если она поддерживает цель — бейдж цели.

**Цель:** PLANNER становится системой:  
**Цель → Задачи + Привычки → Прогресс по цели.**

---

### 2.3. Единая модель и статусы задач

В описании много полей задачи (контекст, приоритет, энергия, подзадачи, т.д.), но не до конца описана **модель статусов и поведения во времени**.

**Требования:**

- Ввести чёткие состояния задачи:
  - `planned` (запланирована),
  - `in_progress` (в фокусе),
  - `done` (выполнена),
  - `skipped` / `moved` (перенесена/пропущена),
  - `overdue` (просрочена).
- Логика:
  - При завершении — зелёная галочка, зачёркивание, фактическое время выполнения.
  - При переносе — обновление даты/слота, отметка в истории.
  - Просроченные задачи:
    - помечать красным/бейджем;  
    - предлагать одним действием перенести на сегодня/завтра (CTA).

**UI-изменения (минимальные):**

- В карточках задач добавить:
  - визуальный признак просрочки;
  - компактное меню «быстрое действие»:
    - «Сдвинуть на завтра»,
    - «Перенести на следующий свободный слот»,
    - «Снять дедлайн».

---

### 2.4. Интеграция Focus Mode с задачами

Сейчас есть кнопка «Фокус» на задачах и отдельный модал Focus Mode. Нужно сделать эту связку **рабочей и консистентной**.

**Требования:**

- При нажатии «Фокус» на задаче:
  - заполнять Focus Mode выбранной задачей (название, длительность, приоритет, энергия),
  - стартовать таймер или готовить старт.
- После окончания фокус-сессии:
  - предложить:
    - «Отметить задачу выполненной»,
    - или «Добавить ещё одну сессию»,
    - или «Перенести остаток времени».
- В статистике продуктивности (для Insights/Analytics):
  - фиксировать связь фокус-сессий с конкретными задачами и целями.

**Цель:** Focus Mode = не просто таймер, а **режим выполнения конкретной задачи**.

---

### 2.5. Календарь как «вторая точка входа»

Calendar View уже описан детально, но нужно уточнить его роль:

**Требование:**

- Календарь — это **альтернативное представление** PLANNER:
  - тот же набор задач/привычек/целей,
  - только в виде сетки/списка по датам.
- Никакой отдельной сущности «событие календаря» не нужно — это:
  - задачи с датой/временем,
  - привычки по дням,
  - вехи целей.

**UI/UX:**

- При тапе по дню в календаре:
  - открывать список задач/привычек/целей на этот день,
  - с возможностью быстро:
    - добавить задачу в этот день,
    - перенести задачу в этот день.

---

### 2.6. AI-рекомендации: единый формат и CTA

Сейчас AI описан текстово (лучшее время, длительность, советы).  
Нужно:

- привести AI-выводы к **единому формату** и
- сделать их **нажимаемыми действиями**.

**Для задач (Add Task / карточка):**

- AI-блок должен:
  - предлагать:
    - рекомендуемое время,
    - длительность,
    - контекст,
    - уровень энергии,
  - иметь кнопку «Применить рекомендации»:
    - проставляет время/длительность/контекст/энергию в форме.
- Для существующей задачи:
  - «AI: Лучше перенести на 14:00–16:00» → CTA: «Перенести».

**Для привычек:**

- AI-рекомендации:
  - время,
  - частота,
  - сложность.
- Кнопка «Применить» → обновляет настройки привычки.

**Для целей:**

- AI-советы:
  - разумный срок,
  - предложенные вехи,
  - рекомендованный темп.
- CTA:
  - «Добавить вехи автоматически»,
  - «Обновить дедлайн».

---

### 2.7. Голосовые команды → реальные действия

Голосовые команды уже описаны. Нужно явно зафиксировать:

- Каждая команда должна мапиться на:
  - создание/изменение задачи/цели/привычки/фокус-режима,
  - или выдачу списка (например: «Что у меня на сегодня?»).
- В коде должна быть:
  - **таблица соответствий** (intent → действие),
  - единое место обработки (например, VoiceIntentHandler).

**Примеры маппинга:**

- «Добавь встречу завтра в 15:00» → Add Task:
  - date = завтра,
  - time = 15:00,
  - context = @работа (по умолчанию).
- «Запусти помодоро» → Focus Mode:
  - техника = Pomodoro,
  - длительность = 25/5 по дефолту.

---

## 3. Уточнения по вкладкам

### 3.1. Tasks — главный рабочий экран

**Итоговая роль вкладки Tasks:**

- Это **центр дневного управления**:
  - показывает задачи по временным блокам,
  - даёт быстрый доступ к фокус-режиму,
  - позволяет быстро переносить/закрывать задачи,
  - показывает связь задач с целями и привычками.

**Добавить:**

- Короткий дневной summary вверху:
  - «Сегодня: 5 задач • 3 привычки • 1 важная цель».
- Быстрые действия над задачами:
  - свайпами или меню: «Сегодня/Завтра/Выходные/Без даты».

---

### 3.2. Goals — стратегия и прогресс

**Роль:**

- Цели — это **стратегический уровень**:
  - показывают, к чему всё идёт.
- Низ лежит на задачах и привычках.

**Добавить:**

- На карточку цели:
  - блок «Следующий шаг»:
    - либо ближайшая задача по этой цели,
    - либо предложение создать такую задачу (CTA).
- Оттуда же:
  - кнопка «Добавить шаг» → модал Add Task с привязкой к цели.

---

### 3.3. Habits — автоматизация поведения

**Роль:**

- Привычки — это **мост между ежедневной рутиной и целями**.

**Добавить:**

- Линк на цель (если привычка создана «под цель»).
- В статистике:
  - вклад: «какие цели эта привычка поддерживает».

---

## 4. Уточнения по модальным окнам

### 4.1. Add Task

- AI-блок должен иметь:
  - предложения,
  - кнопку «Применить».
- При открытии из цели:
  - цель должна быть автоматически установлена.
- При открытии из календаря:
  - дата должна быть предзаполнена выбранным днём.

### 4.2. Add Goal

- При выборе типа **Финансовая**:
  - предусмотреть связку с модулем Finance (позже):
    - счёт/категория,
    - автопрогресс от транзакций.
- При создании цели:
  - рекомендовать создать 1–2 первых задачи и 1 привычку.

### 4.3. Add Habit

- Если привычка создаётся из цели:
  - цель должна быть записана как `linkedGoalId`.
- AI-рекомендации должны корректно применяться (частота, время, сложность).

---

## 5. Интеграции PLANNER с другими модулями

### 5.1. Связь с Finance

- Финансовые цели:
  - могут получать прогресс из модуля Finance (по последствиям).
- PLANNER не обязан вычислять это сам, но:
  - модель цели должна позволять записывать прогресс, пришедший извне.

### 5.2. Связь с Insights

- PLANNER предоставляет:
  - данные о задачах, привычках, фокус-сессиях, прогрессе по целям;
- INSIGHTS использует это для:
  - продуктивность,
  - дисциплина,
  - achieving goals,
  - рекомендации по балансу времени.

### 5.3. Связь с Home

- Home может показывать:
  - summary задач/привычек/целей из PLANNER,
  - быстрые кнопки «Добавить задачу», «Отметить привычку».

---

## 6. Ограничения и требования к реализации

При обновлении кода модуля PLANNER:

1. **Не переписывать всё с нуля.**  
   Расширять существующие сторы, типы и компоненты. Текущий UI и структура остаются в основе.

2. **Сохранять визуальный стиль и UX**, описанные в основной спецификации.

3. **Добавить недостающие слои логики:**
   - связи Tasks↔Goals↔Habits,
   - статусы задач,
   - интеграцию Focus Mode с задачами,
   - единый формат AI-подсказок и их применение,
   - маппинг голосовых команд.

4. **Продолжать поддерживать оффлайн-первый подход**:
   - все сущности должны жить локально,
   - синхронизация/AI могут быть фоном.

5. **Локализация:**  
   все новые тексты (заголовки, подписи, подсказки, AI-сообщения, CTA) — только через i18n-ключи, с переводами на **5 языков**:
   - EN (английский),
   - RU (русский),
   - UZ (узбекский, латиница),
   - TR (турецкий),
   - AR (арабский, RTL).

Этот патч-файл должен использоваться вместе с исходной спецификацией PLANNER и текущим кодом как источник доработок и рефакторинга.
